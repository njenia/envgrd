# Goreleaser configuration for envgrd
project_name: envgrd

# Build configuration
builds:
  - id: envgrd
    main: ./cmd/envgrd
    binary: envgrd
    goos:
      - linux
    goarch:
      - amd64
    env:
      - CGO_ENABLED=1
    # Skip macOS and Windows - CGO cross-compilation from Linux requires native toolchains
    # macOS and Windows users should build from source: make build
    skip:
      - goos: darwin
      - goos: windows
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}
    flags:
      - -trimpath

# Archive configuration
archives:
  - id: default
    format_overrides:
      - goos: windows
        format: zip
    name_template: >-
      {{ .ProjectName }}-{{ .Os }}-{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - LICENSE
      - README.md

# Checksums
checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

# Release configuration
release:
  mode: replace
  header: |
    ## envgrd {{ .Tag }}
    
    Automated release via [GoReleaser](https://goreleaser.com)
  footer: |
    ---
    
    Built with ❤️ by [GoReleaser](https://github.com/goreleaser/goreleaser)
  
  # Mark pre-releases (tags containing '-')
  prerelease: auto

# Changelog
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - Merge pull reenvgrdst
      - Merge branch

