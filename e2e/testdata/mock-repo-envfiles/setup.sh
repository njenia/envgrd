#!/bin/bash

# Shell script with exported environment variables
export SHELL_SCRIPT_VAR=shell-value
export SHELL_API_KEY=shell-api-key
export SHELL_ENV=development

# Regular variables (not exported) should be ignored
LOCAL_VAR=local-value

